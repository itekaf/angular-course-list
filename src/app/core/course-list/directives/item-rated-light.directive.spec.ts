import { ItemRatedLightDirective } from './item-rated-light.directive';

describe('ItemBackgroundColorDirective', () => {
	it('should create an instance', () => {
		const directive = new ItemRatedLightDirective();
		expect(directive).toBeTruthy();
	});
});
